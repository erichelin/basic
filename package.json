{
  "name": "basic",
  "version": "1.0.0",
  "description": "",
  "main": "app.ts",
  "scripts": {
    "test": "mocha --require ts-node/register src/**/**/*.ts",
    "build": "tsc -p .",
    "start": "tsc -p . && node build/app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/hapi": "^16.0.4",
    "hapi": "^16.1.1"
  }
}
